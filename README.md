# helmet_detection

### Overview

This project is a deep learning-based object detection system that detects whether a person is wearing a helmet or not.
It uses a Faster R-CNN model built with PyTorch to perform image-based detection.

The goal of this project is to improve safety monitoring in areas such as roads or construction sites.
